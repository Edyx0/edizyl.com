{
  "name": "edizy-spotify-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "get-refresh-token": "node scripts/get-refresh-token.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
